# Agent Hub — Netlify config
# publish = "pages" → pages/*.html served at root, pages/files/* served at /files/*

[build]
  publish = "pages"

[[headers]]
  for = "/files/*"
  [headers.values]
    Cache-Control = "public, max-age=3600"
